# Rivendell

## Building

Drop a `google-services.json` at `app/android/app/` before building
(generate one from the Google Cloud Console — Android client, package
`com.rivendell.app`). Required by the `com.google.gms.google-services`
Gradle plugin; gitignored so it stays out of the public repo.
