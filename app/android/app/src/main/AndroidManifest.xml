<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Background audio (FR-1.1.4): keep playback alive while the screen is off,
         and run it as a foreground media service on Android 14+ (API 34). -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- In-app microphone capture (FR-1.1.3). The `record` plugin requests the
         runtime permission; this declaration is what makes the grant possible. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Scheduled task reminders (FR-1.4.2, T5.3). POST_NOTIFICATIONS surfaces
         them on Android 13+; SCHEDULE_EXACT_ALARM lets AlarmManager fire at the
         exact due-day moment (granted by default; user-revocable on 12+, where
         we fall back to inexact scheduling). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- AnkiDroid content-provider access (FR-1.3.3). Without this declaration
         Android never grants Rivendell the custom permission, so every query
         (deckList / addNote / addMedia) is rejected with "Permission not granted
         for: CardContentProvider.query". The user separately allows Rivendell
         in AnkiDroid → Settings → API the first time the API is used. -->
    <uses-permission android:name="com.ichi2.anki.permission.READ_WRITE_DATABASE"/>
    <application
        android:label="Rivendell"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Background media-session service + media-button receiver for
             audio_service (FR-1.1.4). foregroundServiceType=mediaPlayback is
             mandatory on Android 14+ for a foreground media service. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
        <!-- FileProvider exposing cached AI concept images (under filesDir/
             ai_images/) so AnkiDroid can import them into its media collection
             via addMediaFromUri (FR-1.3.4, T4.4). grantUriPermissions lets the
             code hand a temporary read grant to com.ichi2.anki per import. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
              android:name="android.support.FILE_PROVIDER_PATHS"
              android:resource="@xml/file_paths" />
        </provider>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- AnkiDroid (com.ichi2.anki) visibility on Android 11+ (API 30): the
             export flow's isInstalled() check and the "open AnkiDroid" CTA both
             need to resolve the package. -->
        <package android:name="com.ichi2.anki"/>
    </queries>
</manifest>
