### Dart / Flutter
testenv
# Build & dependency artifacts
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/
pubspec.lock

# Note: generated sources (*.g.dart, *.freezed.dart) are committed on purpose so
# `flutter test` runs without a codegen step in CI (see .claude/skills/setup.md).

# Test coverage output
coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

### Secrets — never commit (see .claude/skills/setup.md)
env.json
.env
.env.*
*.keystore
*.jks

### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk
